package com.ms.tomf.Objects.MapObjects
{
	import com.ms.tomf.Screens.InGame.InGame;
	
	import flash.display.MovieClip;
	
	public class SpeechBubble extends MovieClip
	{
		public function SpeechBubble()
		{
			/*if(InGame.inGameContent.map.x -  InGame.inGameContent.player.x > 1700 && InGame.inGameContent.map.x < 2200)
			{
				this.visible = true;
			}
			else{this.visible = false;}*/
		}
	}
}